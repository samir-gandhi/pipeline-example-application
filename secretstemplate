# Will need the following: # 
# region            = "{{ NorthAmerica | Canada | Asia | Europe }}"
# pingone_environment_id      = "{{adminEnvId}}"
# admin_user_id     = "{{adminUserId}}"
# license_id        = "{{licenseId}}"
# worker_id         = "{{workerId}}"
# worker_secret     = "{{workerSecret}}"
# admin_username    = "{{adminUsername}}"
# admin_password    = "{{adminPassword}}"

# one of: NorthAmerica | Canada | Asia | Europe
export TF_VAR_pingone_region=''
export TF_VAR_pingone_environment_id=''
export TF_VAR_pingone_admin_user_id=''
export TF_VAR_pingone_license_id=''
export TF_VAR_pingone_worker_id=''
export TF_VAR_pingone_worker_secret=''
export TF_VAR_pingone_admin_username=''
export TF_VAR_pingone_admin_password=''

## from CICD repo
# aws credentials and information for dev environment
export AWS_ACCESS_KEY_ID=''
export AWS_SECRET_ACCESS_KEY=''
# bucket name for storing state - for example, 'ping-terraform-demo-mycompany'
export TF_VAR_tf_state_bucket=''
# region for storing state - for example, 'us-west-1'
export TF_VAR_tf_state_region=''
# DO NOT EDIT
export TF_VAR_tf_state_key="dev/$(git rev-parse --abbrev-ref HEAD)/terraform.tfstate"